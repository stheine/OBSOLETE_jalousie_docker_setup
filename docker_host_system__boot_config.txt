# Enable the optional hardware interface, SPI
dtparam=spi=on

# Enable UART
enable_uart=1

# Disable bluetooth
dtoverlay=pi3-disable-bt

# max_usb_current=1



# und aus /etc/inittab die referenz auf ttyAMA0 auskommentieren

# und sudo systemctl disable hciuart

# und aus /boot/cmdline.txt die referenz auf ttyAMA0 loeschen
